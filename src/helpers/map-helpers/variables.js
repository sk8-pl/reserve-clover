const modeGeometry = {
  mode: null,
};
// eslint-disable-next-line no-undef
export const myPointsHeight = new Cesium.EntityCollection();

export const isPick = {
  value: false,
  polygonVertices: [],
};

export const pickedEntityObject = {
  arrayPoints: [],
};

export const nullHeightPZObject = {
  nullHeightPZ: null,
};

export default modeGeometry;
